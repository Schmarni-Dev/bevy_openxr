use bevy::prelude::*;

use crate::{future_util::ToJsFuture};
pub struct WxrInstancePlugin;
// use wasm_bindgen_futures::JsFuture

impl Plugin for WxrInstancePlugin {
    fn build(&self, app: &mut App) {}
}

